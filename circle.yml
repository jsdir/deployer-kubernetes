machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/docker"
  override:
    - docker info
    - if [[ -e ~/dind-kubernetes/image.tar ]]; then docker load -i ~/docker/dind-kubernetes.tar; fi
    - docker pull llamashoes/dind-kubernetes
    - mkdir -p ~/docker; docker save llamashoes/dind-kubernetes > ~/docker/dind-kubernetes.tar
    - if [[ -e ~/kube/image.tar ]]; then docker load -i ~/docker/kube.tar; fi
    - docker pull fandekasp/kube
    - mkdir -p ~/docker; docker save fandekasp/kube > ~/docker/kube.tar

test:
  override:
    - ./scripts/start-cluster.sh
